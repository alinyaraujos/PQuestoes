{% extends "layout.twig" %}

{% block styles %}
    <link rel="stylesheet" type="text/css" href="{{base_url()}}/assets/questao/css/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="{{base_url()}}/assets/questao/css/freelancer.css">
    <link rel="stylesheet" type="text/css" href="{{base_url()}}/assets/questao/font-awesome/css/font-awesome.min.css">
    <link rel="stylesheet" type="text/css" href="{{base_url()}}/assets/questao/css/freelancer.css">
    <link rel="stylesheet" type="text/css" href="{{base_url()}}/assets/questao/css/cssTela.css">
<style>

</style>

{% endblock %}

{% block title %}
Gabarito
{% endblock %}

{% block conteudo %}
<br><br><br><br><br><br><br> 
<div class="container">
    <div class="row">
        <div class="col-lg-12 text-center">
            <h2>Adicionar Gabarito</h2>
            <hr class="star-primary">
        </div>
                
                <form name="resultado" action="/addGabarito" method="post">
                <input type="hidden" name="nomeArquivo" value="{{idArquivo}}" >
                 <input type="hidden" name="quantidade" value="{{quantidade}}" >
            {% for item in 1..quantidade %}
                {{item}}. 
                    {% for item2 in letra %}
                        {{item2}}.  <input type="radio" name="q{{item}}" value="{{item2}}">
                    {% endfor %}
                    <br>
            {% endfor %}

                     <center> <input type="submit" value="Corrigir e Adicionar Gabarito" class="btn btn-primary"></center>
                </form>

        </div>
   </div>


{% endblock %}

{% block scripts %}




{% endblock %}

